def process_text(content: str) -> str:
    """
    Dummy text processor that just returns the input text.
    In the future, this will handle PDF extraction and text processing.
    """
    return content 